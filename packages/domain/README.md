# HINTO Domain

This package contains vendor-neutral domain types and rules for the restart architecture.

Current scope is limited to the first vertical slice:

- profile privacy and capability resolution
- situationship ordering and reorder validation
- viewer and audience/access modeling for situationship flows

This package should remain independent of:

- Amplify models
- donor-table naming
- provider OAuth mechanics
- framework-specific HTTP concerns
